language: java
jdk:
 - oraclejdk8

sudo: false

addons:
  sonarqube: true

script:
  # other script steps might be done before running the actual SonarQube analysis
  - sonar-scanner -Dsonar.login=e89436ee9c369bfec14d0e839054bab826e5c39f